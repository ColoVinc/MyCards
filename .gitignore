node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Config locale di Claude Code (launch.json, settings.local.json): non condivisa
.claude/

# Upload immagini in sviluppo locale (in produzione si usa Vercel Blob)
public/uploads
